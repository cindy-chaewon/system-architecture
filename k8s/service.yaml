apiVersion: v1
kind: Service
metadata:
  name: todo-svc
  namespace: todo
spec:
  type: NodePort
  selector:
    app: todo-web
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      nodePort: 30080
